# Remanga Secret Tools

Клиентская часть (Frontend) проекта **Remanga Secret Tools**.
Веб-приложение, предоставляющее пользователям [remanga.org](https://remanga.org) неофициальный функционал и инструменты.

## 🚀 Возможности

*   **Просмотр скрытых карт**: Интерфейс для просмотра карт из "Рандомного пака", которые скрыты на официальном сайте.
*   *(В планах)*: Расширение набора инструментов.

## ⚙️ Установка и запуск

> Для работы с проектом требуется установленный **Flutter** (Stable channel).

### 1. Настройка окружения

#### 1.1 Настройка конфигурации
В корне проекта создайте файл `.env`, используя пример конфигурации:

```bash
cp .env.example .env
```

Заполните `.env` актуальными данными (URL бэкенда, ссылки и прочее).

#### 1.2 Скачивание зависимостей

```bash
flutter pub get
```

#### 1.3 Генерация файлов

```bash
dart run build_runner build --delete-conflicting-outputs
```

### 2. Сборка (Release)

Для сборки WEB-приложения воспользуйтесь командой:

```bash
flutter build web --release --no-wasm-dry-run --dart-define-from-file=.env
```

Результат сборки будет находиться в директории `build/web`.

Для деплоя можно удалить папку canvaskit из `build/web`.