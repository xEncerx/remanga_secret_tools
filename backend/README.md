# Remanga Secret Tools (Backend)

Бэкенд-сервис для проекта **Remanga Secret Tools**.
Основная цель приложения — предоставить пользователям [remanga.org](https://remanga.org) функционал, который официально не поддерживается сайтом.

## 🚀 Текущий функционал

*   **Просмотр скрытых карт "Рандомного пака"**: Инструмент позволяет просматривать карты, которые скрыты в интерфейсе сайта.
*   *(В планах)*: Добавление новых инструментов и фич.

## ⚙️ Установка и запуск

> Проект полностью контейнеризирован. Для запуска требуется установленный **Docker** и **Docker Compose**.

### 1. Настройка окружения

В корне директории `backend` создайте файл `.env` на основе примера:

```bash
cp .env.example .env
```

Откройте `.env` и заполните необходимые переменные конфигурации (доступы к БД, Redis, настройки API).

### 🚀 2. Запуск

Соберите и запустите контейнер одной командой:

```bash
docker-compose up -d --build
```

После успешного запуска API будет доступно по адресу: `http://localhost:8080`