import 'package:dart_frog/dart_frog.dart';

Response onRequest(RequestContext context) {
  return Response(
    body:
        'Welcome to oreshka.me! An unofficial remanga API that provides access to many secret and unique features that are not available on the official website.',
  );
}
